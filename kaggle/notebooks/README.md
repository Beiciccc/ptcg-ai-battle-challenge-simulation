# Kaggle Notebooks

This directory stores notes and small helper files for Kaggle notebook runs.
Large outputs should be downloaded to `artifacts/` or `data/raw/` locally and
kept out of git unless they are compact, public experiment summaries.
